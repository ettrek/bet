Во время загрузки таблицы матчей отображается
http://rb.oddsportal.com/x/oddsportal180128102849/img/ajax-loader-orange.gif
А инициатором WebSocketConnection-Secure является pushclient-180128102849.js

Далее были найдены еще несколько *.js скриптов, содержащих в своем имени 
число `180128102849`. Все они были сохранены в `tests/js/x/` и подключены в 
`index.html`.

При загрузке `index.html` в браузере, в консоли выдает ошибку, связанную с 
`this.initBookmakers()` в файле `global-180128102849.js:1`.

Ошибка устранена путем изменения порядка загрузки JS скриптов.

